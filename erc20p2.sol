// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.18;

import "tokens_practice/erc20p1.sol";


contract DecentralizedStakingPlatform {

    // ERC20P1 token;

    // struct Staker{
    //     address staker;
    //     mapping(address => uint ) stakedAmount;
    //     uint totalStakeAmount;
    // }

    // constructor(address tokenAddress) {
    //     token =ERC20P1(tokenAddress);
    // }



    // mapping(address => Staker) public Stakes;
    // function stake(uint amount) public {
    //     require(amount > 0 , "Amount Must Be Greater Than Zero");
    //     token.transferFrom(msg.sender,address(this),amount);
    //     Staker storage stk = Stakes[msg.sender];
    //     stk.staker = msg.sender;
    //     stk.stakedAmount[msg.sender] = amount;
    //     stk.totalStakeAmount += amount;
    // }

    

}